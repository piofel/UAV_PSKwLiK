function a = unwrap_angle(a,a_prev)
    % Formats angle a in the interval [-pi,pi] so that it can lie in [0, Inf) or (-Inf,0]
    % a_prev = the angle in the previous iteration
    
    TODO
end
